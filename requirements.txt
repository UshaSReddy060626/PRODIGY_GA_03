# This project uses only the Python standard library (re, random, collections,
# argparse, unittest) -- no external dependencies are required to run it.
#
# The following are optional, only needed if you want to run tests via
# pytest instead of the built-in unittest runner, or check code style.
pytest>=7.4.0
